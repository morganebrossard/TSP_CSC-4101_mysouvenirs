{% extends 'base.html.twig' %}

{% block title %}Edit Album{% endblock %}

{% block main %}
    <h1>Editer l'album</h1>

    <table class="table">

        {{ include('album/_form.html.twig', {'button_label': 'Sauvegarder le changement'}) }}
        &nbsp
        {{ include('album/_delete_form.html.twig') }}
        &nbsp
        {# On choisit un style noir pour toutes les actions du type "reotur à la liste" #}
        <a href="{{ path('app_album_index') }}" class = "btn btn-dark btn-sm" role="button">Retour à la liste</a>
    
        </table>

    

{% endblock %}
