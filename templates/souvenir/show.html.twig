{% extends 'base.html.twig' %}

{% block title %}Souvenirs{% endblock %}

  {% block main %}
      <h1>" {{ souvenir.title }} "</h1>

     <table class="table table-light">
          <tbody>
              <tr>
                  <th>Id</th>
                  <td>{{ souvenir.id }}</td>
              </tr>
              <tr>
                  <th>Titre</th>
                  <td>{{ souvenir.title }}</td>
              </tr>     
                <tr>
                  <th>Date</th>
                  <td>{{ souvenir.date ? souvenir.date|date('Y-m-d H:i:s') : '' }}</td>
              </tr>
                <tr>
                  <th>Album</th>
                  <td>{{ souvenir.album }}</td>
              </tr>
                      


          </tbody>
      </table>

      <a href="{{ path('app_souvenir_index') }}">back to list</a>

        <a href="{{ path('app_souvenir_edit', {'id': souvenir.id}) }}">edit</a>

    {{ include('souvenir/_delete_form.html.twig') }}

 {% endblock %} {# main #}



