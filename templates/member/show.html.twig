{% extends 'base.html.twig' %}

{% block title %}Members{% endblock %}

  {% block main %}
      <h1>Bienvenue chez : {{ member.name }} </h1>

         {% dump member %}

      <table class="table table-light">
      <thead>
        <tr>
            <th>Albums</th>
            <th> &nbsp;</th>

        </tr>
    </thead>
    <tbody>    
                   
         {% for alb in member.album %}
         <tr>
                <td>{{ alb }}</td>
                <td> <a class="btn btn-outline-dark btn-sm " href="{{ path('app_album_show', {'id': alb.id}) }}">Voir cet album</a></td>
         
         </tr>
         {% endfor %} {# albums #}

          </tbody>
      </table>


    <a href="{{ path('app_album_new', {'id': member.id}) }}">Ajouter un nouvel album</a>
    
    <a href="{{ path('app_member_index') }}">Retour à la liste</a>

    <a href="{{ path('app_member_edit', {'id': member.id}) }}">Editer</a>

    {{ include('member/_delete_form.html.twig') }}
    

  {% endblock %} {# main #}




