{% extends 'base.html.twig' %}

{% block title %}Members{% endblock %}

  {% block main %}
      <h1>Bienvenue chez : {{ member.name }} </h1>

         {% dump member %}

      <table class="table table-light">
      <thead>
        <tr>
            <th>Albums</th>
            <th> &nbsp;</th>

        </tr>
    </thead>
    <tbody>    
                   
         {% for alb in member.album %}
         <tr>
                <td>{{ alb }}</td>
                <td> <a class="btn btn-outline-dark btn-sm " href="{{ path('app_album_show', {'id': alb.id}) }}">Voir cet album</a></td>
         
         </tr>
         {% endfor %} {# albums #}

          </tbody>
      </table>


      <table class="table">
    <a href="{{ path('app_member_edit', {'id': member.id}) }}" class = "btn btn-secondary btn-sm" role="button">Editer ce membre</a>
    &nbsp
    <a href="{{ path('app_album_new', {'id': member.id}) }}" class = "btn btn-success btn-sm" role="button">Ajouter un nouvel album</a>
  </table>

    <table class="table">
    <a href="{{ path('app_member_index') }}" class = "btn btn-dark btn-sm" role="button">Retour à la liste</a>
    &nbsp
    {{ include('member/_delete_form.html.twig') }}
    </table>
    

  {% endblock %} {# main #}




